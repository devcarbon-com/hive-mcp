apiVersion: v1
kind: Service
metadata:
  name: ollama-embed
  namespace: hive
  labels:
    app: ollama-embed
    component: embeddings
spec:
  type: ClusterIP
  selector:
    app: ollama-embed
  ports:
    - port: 11434
      targetPort: 11434
      protocol: TCP
      name: http
